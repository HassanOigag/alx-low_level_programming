#include "main.h"
/**
*_abs - computes the absolute value of a number
*@a: the number int question
*Return: retuns the absolute value of the number n
*/
int	_abs(int a)
{
	if (a < 0)
		return (-a);
	return (a);
}
